import React from 'react'
import { useContext } from 'react';
import { AnimalCrossingContext } from '../../context/context.jsx';

const Fish = () => {

  // const { fish } = useContext(AnimalCrossingContext);

  return (
    <div>Fish</div>
  )
}

export default Fish